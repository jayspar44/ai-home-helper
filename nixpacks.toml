[build]
cmd = "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../frontend && npm run build"

[start]
cmd = "node backend/server.js"

[variables]
NODE_ENV = "production"
